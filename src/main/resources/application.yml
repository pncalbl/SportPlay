spring:
  banner:
    location: banner.txt
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/sport_play?useUnicode=true&characterEncoding=UTF-8&useSSL=true&serverTimezone=GMT
  gson:
    date-format: yyyy-MM-dd HH:mm:ss

mybatis:
  type-aliases-package: com.pncalbl.sportplay.pojo
  mapper-locations: classpath:mapper/*.xml

#是否开启 swagger-ui
swagger:
  enabled: true

server:
  port: 9000

